#include<bits/stdc++.h>
using namespace std;
int main(){
	//for input
	int n,m;
	map<string,int> m;
	cin>>n>>m;
	while(n--){
				string name;
		int no_of_skills;
		cin>>name>>no_of_skills;
		m.insert({name,no_of_skills});
		while(no_of_skills--){
			map<string,int> m1;
			string skill;
			int skill_level;
			m1.insert({skill,skill_level});
		}
	}
	while(m--){
		string projname;
		int duration,score,bestbefore,req;
		cin>>projname>>duration>>score>>bestbefore>>req;

	}
}